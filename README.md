
```bash
>> cmake -DFULL_BUILD=OFF -DTARGET_i386=OFF ..
```

```bash
>> make -j48
>> make -j48 lkm
```

```bash
>> sudo ln -s /usr/bin/make /usr/bin/gmake
>> yay -S libavresample
>> sudo ln -s /lib64/dbus-1.0/include/dbus/dbus-arch-deps.h ./dbus-arch-deps.h
>> sudo mkdir -p /usr/local/libexec/darling/private
>> sudo make install
>> sudo make lkm_install
```
